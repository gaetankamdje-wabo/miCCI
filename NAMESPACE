# Strategies (single-encounter)
export(cci_gold)
export(cci_interval)
export(cci_probabilistic)
export(cci_mi)
export(cci_bayesian)

# Strategies (vectorised batch - the form most users actually call)
export(cci_gold_batch)
export(cci_interval_batch)
export(cci_probabilistic_batch)
export(cci_mi_batch)
export(cci_bayesian_batch)

# Meta learner
export(cci_meta_fit)
S3method(predict, SL_passthrough)

# Reference data + lookup engine (needed by every strategy)
export(load_quan_map)
export(load_destatis)
export(precompute_lookups)

# Imports
importFrom(data.table, data.table, fread, fwrite, as.data.table, setDT,
           setkey, setnames, setorder, setorderv, setattr, setcolorder,
           rbindlist, dcast, melt, copy, fifelse, fcase, uniqueN, frank,
           CJ, .N, .SD, set, is.data.table)
importFrom(jsonlite, fromJSON, toJSON)
importFrom(stats, predict, setNames, var, median, quantile, sd,
           rgamma, gaussian)
importFrom(utils, download.file, head)
